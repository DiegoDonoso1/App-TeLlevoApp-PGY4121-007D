import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'presentacion1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
